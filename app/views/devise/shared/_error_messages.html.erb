<% if resource.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <% if resource.errors.full_messages.length != 1 %>
        <ul class="mb-0">
          <% resource.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% else %>
        <%= resource.errors.full_messages.first %>
      <% end %>
    </div>
  </div>
<% end %>
